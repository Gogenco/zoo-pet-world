# Zoo Pet World — Stage 3.2 Guide Button Hotfix

Что исправлено:
- Кнопка `?`/`❔` помощника теперь поднята выше всех игровых слоёв (`z-index` max-safe).
- Добавлен жёсткий bind на `click`, `pointerup`, `touchend`.
- Добавлен document-level capture fallback: если поверх кнопки были странные обработчики, клик всё равно открывает помощника.
- Добавлен safe fallback: если конкретный guide flow сломался или selector не найден, показывается обычная карточка подсказки, а не “ничего”.
- При открытом помощнике кнопка `?` отключается, чтобы не было двойных кликов.

Не трогалось:
- игровые механики;
- магазин;
- задания;
- раскраска по логике;
- Performance mode;
- rewarded-ad hotfix.

Проверки:
- JS syntax check: OK.
- `.git`: нет.
