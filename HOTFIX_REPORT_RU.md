# Zoo Pet World — Stage 3.1 Hotfix

Дата: 2026-06-21

## Что исправлено

1. Rewarded Ad / бонус подсказок больше не выводится обычным текстом справа от игры.
   - Добавлены стили `.rewarded-ad-overlay`, `.rewarded-ad-card`, progress bar и действия.
   - Оверлей теперь фиксированный, центрированный и появляется только при `.show`.

2. Кнопка помощника `❔` сделана устойчивее.
   - Явно привязан `onclick` к `guideFab`.
   - Guide-функции экспортированы в `window`.
   - Если selector шага не найден, manual guide больше не выглядит сломанным: показывает безопасный текст вместо кривой рамки/молчания.

3. Сохранены прошлые фиксы:
   - Zoo Pet World title/name;
   - Performance mode;
   - back navigation;
   - coloring loading;
   - Stage 3 data-driven guide.

## Проверки

- `.git`: нет
- JS syntax inline: OK
- JS syntax external assets/js: OK
- `assets/inline`: 54 файла
- `coloringImages.js`: 30 встроенных раскрасок

## Что проверить на сайте

1. Открыть любую мини-игру и нажать бонус/рекламу — окно должно быть по центру, не текстом справа.
2. Нажать `❔` на главном меню, в комнате питомца, раскраске и мини-играх.
3. Если гид не нашёл элемент — он должен показать текст, а не молчать и не рисовать кривую рамку.
